<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artist Albums</title>
</head>
<body>
    <h1>Albums</h1>
    <ul>
        {{#each albums}}
            <li>
                <img src="{{images.[0].url}}" alt="{{name}} Cover" width="300">
                {{name}}
                <a href="/tracks/{{id}}">View Tracks</a>
            </li>
        {{/each}}
    </ul>
</body>
</html>
